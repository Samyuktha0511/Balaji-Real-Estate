{
  "name": "balaji-realestate-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute balaji-realestate --file=src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute balaji-realestate --remote --file=src/db/schema.sql",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "itty-router": "^5.0.17"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.5.2",
    "wrangler": "^3.72.2"
  }
}
